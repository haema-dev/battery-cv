name: 02_Stage2_Evaluation_and_Heatmap
on:
  push:
    branches:
      - tk_test_4
  workflow_dispatch:

jobs:
  inference:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Install Azure ML CLI
        run: |
          az extension add -n ml --yes
          az config set extension.dynamic_install_allow_preview=true

      - name: Configure Azure ML
        run: |
          az configure --defaults \
            group=${{ secrets.AZURE_RESOURCE_GROUP }} \
            workspace=${{ secrets.AZURE_WORKSPACE }}

      - name: Setup Environment
        run: |
          python -m pip install --upgrade pip
          pip install azure-ai-ml azure-identity anomalib==1.1.3 loguru mlflow

      - name: Register Model from Stage 1
        id: register_model
        run: |
          echo "🚀 Registering model from Job: dynamic_juice_f035fsld3z"
          # 1단계 결과물을 'battery_fastflow_model'이라는 이름으로 등록하고 결과를 JSON으로 받습니다.
          REG_OUTPUT=$(az ml model create --name battery_fastflow_model \
            --path azureml://jobs/dynamic_juice_f035fsld3z/outputs/outputs/model.pt \
            --type custom_model -o json)
          
          # 등록된 모델의 버전(version)을 추출하여 환경 변수에 저장합니다.
          VERSION=$(echo $REG_OUTPUT | jq -r '.version')
          echo "MODEL_VERSION=$VERSION" >> $GITHUB_ENV
          echo "✅ Model Registered Successfully. Version: $VERSION"

      - name: Run Stage 2 Evaluation
        run: |
          echo "📊 Running Stage 2 with Model Version: ${{ env.MODEL_VERSION }}"
          # 등록된 특정 버전을 직접 지정하여 inference-job.yml을 실행합니다. (@latest 지연 방지)
          JOB_NAME=$(az ml job create --file inference-job.yml \
            --set inputs.model.path=azureml:battery_fastflow_model:${{ env.MODEL_VERSION }} \
            --query name -o tsv)
          
          echo "JOB_NAME=$JOB_NAME" >> $GITHUB_ENV
          echo "✅ [Stage 2] Evaluation Job Submitted: $JOB_NAME"
